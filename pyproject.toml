[tool.ruff]
select = ["E", "F", "I"]

exclude = [
    ".git",
    ".idea",
    ".run",
    ".pytype/",
    ".venv",
    "3rdparty",
    "venv",
]
line-length = 120

[tool.ruff.isort]
relative-imports-order = "closest-to-furthest"
known-local-folder = ["pycheribuild"]
